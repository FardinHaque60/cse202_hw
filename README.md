# cse202_hw

hw files for cse 202 algorithms

## create python venv

1. `python3 -m venv venv` to create venv
2. `source venv/bin/activate` to activate venv
3. `pip install -r requirements.txt` to install requirements

`deactivate` to deactivate venv once done using
